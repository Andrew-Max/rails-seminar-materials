<div class="message">
  <div class="meta-data">
    <div class="gravatar">
      <%= gravatar_link(message.author) %>
    </div>
    <div>
      To: <%= link_to(message.owner.name, user_path(message.owner)) %>
    </div>
    <div class="info">
      <p>From: <%= link_to message.author.name, user_path(message.author) %></p>
      <p><%= message.created_at.to_datetime.to_s(:easy) %></p>
    </div>
  </div>
  <div class="message-body">
    <%= message.body %>
  </div>
</div>