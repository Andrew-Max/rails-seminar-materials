<!-- this partial is an individual user id card which is displayed on the home page -->
<div class="id-card">
  <div class="avatar">
    <%= gravatar_link(user) %>
    <!-- look in application helper to see how gravatar link works -->
  </div>
  <!-- look at the rails docs to see how link_to works -->
  <%#= link_to <<<replace with the name of user : this isthe text of the link >>>, user_path(user), class: "name" %>
  <%= link_to("Be Me", root_url(current_user_id: user), class: "btn btn-primary btn-success") %>
</div>