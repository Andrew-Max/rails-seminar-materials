<!-- ********************************************************************
Description: the new users form template
Routing info: Rendered by the new method of the usersController

This is the form to create a new user.

This form uses the rails "form_for" helper to generate a HTML form for
creating a new user in our database

The helper intelligently knows which route to submit the form to based on the class of the object we pass into it (User in this case). Therefore this form will submit to the create method in the UsersController

It will pass the values entered in the fields  to the controller method via a hash called params when it is submitted


more info for form_for at http://apidock.com/rails/ActionView/Helpers/FormHelper/form_for
******************************************************************** -->



<p>Go to the views/users/new.html.erb and follow the hints to create the functionality on this page. Delete this text when you are finished</p>

<div class="signup-form">
  <%= form_for(@new_user) do |f| %>
    <% if @new_user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@new_user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
        <% @new_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>

    <!-- <<< create another div with class field>>> -->
    <!-- <<< Inside the div create another form label and form text field for the attribute email >>> -->


    <!-- <<< create another div with class actions>>> -->
    <!-- <<< Inside the div create a form submit and set its class argument equal to class: "btn btn-primary" >>> -->
    <!-- <<< Look online on the docs for form_for for help with the syntax if you are unsure >>> -->
  <% end %>
</div>
